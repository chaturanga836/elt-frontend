services:
  frontend:
    container_name: etl-frontend
    build:
      context: .
      args:
        - NEXT_PUBLIC_BUILD_ID=${BUILD_ID:-development}
    ports:
      - "3000:3000" # Change this line