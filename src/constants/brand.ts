/** Customer-facing product brand (UI). */

export const BRAND_NAME = 'DT-Orch';

export const BRAND_TAGLINE =
  'Orchestrate your data stack — on your infrastructure.';

/** Set when logo asset is added under public/ (e.g. '/brand/logo.svg'). */
export const BRAND_LOGO_SRC: string | null = null;

export const BRAND_LOGO_ALT = `${BRAND_NAME} logo`;
