/** Customer-facing product brand (UI). */

export const BRAND_NAME = 'DT Orch';

export const BRAND_TAGLINE =
  'Databases, APIs, and workflows — self-hosted on infrastructure you control.';

/** Static SVG mark under public/ (favicon + external use). */
export const BRAND_LOGO_SRC = '/brand/dt-orch-logo.svg';
export const BRAND_ICON_SRC = '/brand/dt-orch-icon.svg';

/** Logo image already includes the wordmark; hide duplicate text beside it. */
export const BRAND_LOGO_INCLUDES_WORDMARK = true;

export const BRAND_LOGO_ALT = `${BRAND_NAME} logo`;

/** Horizontal logo aspect ratio (width / height) for layout. */
export const BRAND_LOGO_ASPECT = 3.35;
